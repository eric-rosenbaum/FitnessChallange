# Fitness Challenge — iOS

Swift/iOS app (to be added). Shares the same Supabase backend as the web app.

Open this folder when you create the Xcode project so the project lives at `ios/YourAppName/`.
